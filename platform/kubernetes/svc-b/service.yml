apiVersion: v1
kind: Service
metadata:
  name: svc-b
  namespace: microservices
  labels:
    app: svc-b
    version: "1.0.0"
    managed-by: kubectl
spec:
  type: ClusterIP
  selector:
    app: svc-b
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP

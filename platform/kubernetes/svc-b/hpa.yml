apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: svc-b-hpa
  namespace: microservices
  labels:
    app: svc-b
    version: "1.0.0"
    managed-by: kubectl
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: svc-b
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
